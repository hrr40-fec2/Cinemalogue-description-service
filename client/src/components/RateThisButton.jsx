import styled from 'styled-components';

const RateThisButton = styled.button`
  color: white;
  background-color: transparent;
  border: none;
  border-left: 1px solid grey;
`;

export default RateThisButton;